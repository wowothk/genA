### prosedur 2 ###
from prosedur3 import int_decode

for j in range(len(J)):
    v[j]=0 if b[j] == 0

from prosedur2 import decoding

for k in range(len(K)):
    v[k]=0 if b[k]==0

from prosedur1 import decoding


# calculate the objective function
